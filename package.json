{
  "name": "carousel-component-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "./node_modules/.bin/live-server --no-browser --mount=/:src --entry-file=index.html --watch=src,package.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "dependencies": {
  }
}
